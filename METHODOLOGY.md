# 指标口径与隐私说明

## 统计起点

系统从使用统计功能正式上线时重新起算，不导入 Nginx、PM2 或其他历史日志。每份快照内的 `trackingStartedAt` 是可核对的正式起点。

## 指标定义

- 访问会话：一个浏览器标签页在该标签页会话中最多计一次；不是独立用户数。
- 估算活跃设备：同一浏览器在同一北京时间日期最多计一次，只是设备侧估算，不代表真实人数。
- 栏目查看：首页、时间轴、通知、资料、意见板在一次标签页会话中首次打开的次数。
- 任务详情展开：完整说明被展开的次数，不记录具体任务。
- 资料链接点击：资料链接被点击的次数，不记录具体链接。
- 摘要复制：事项摘要被复制的次数，不记录具体事项。
- 链接复制：事项独立链接被复制的次数，不记录具体事项。
- 独立链接打开：通过事项独立链接打开页面的次数，不记录具体事项。
- 任务图片生成：浏览器成功生成一张事项 PNG 图片的次数；不代表图片已经保存、分享或被他人查看，也不记录具体事项。
- 任务发布：按北京时间统计当天首次公开发布的事项数量。
- 任务修改：按北京时间统计当天发生公开变更的不同事项数量；同一事项同日多次修改只计一次。

“日均估算活跃设备”只从正式统计起点开始计算有效天数。2026 年 9 月证据从 9 月 3 日开始，是不完整统计期；其中 9 月 10 日前的上线验证流量无法与同学访问完全区分，因此如实保留，不做主观扣减。2026 年 10 月是第一个完整自然月。

“面向 30 人班级”只说明服务范围。本项目不据此计算或宣称“30 人使用率”。

## 隐私边界

服务器只保存北京时间日期、固定指标名称、汇总次数和更新时间。系统不保存逐条使用事件、IP、User-Agent、来源网址、设备编号、账号、任务编号、意见编号或个人浏览轨迹；匿名意见与使用统计无法关联。

## 封存与更正

每月 2 日北京时间 03:17 封存上月快照，已登记快照不可覆盖。内容数量按上月最后一天 23:59:59（UTC+8）的状态计算，不读取封存当天的实时状态。每月 3 日北京时间 10:30 由 GitHub Actions 获取文件、验证服务端哈希并提交。如果发现口径或展示错误，新增 `corrections/YYYY-MM-说明.md`，不得改写原快照。
